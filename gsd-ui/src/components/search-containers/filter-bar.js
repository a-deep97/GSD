import React from 'react';
import { Box } from '@mui/material';

const FilterBar = () => {
    return (
        <Box sx={{
            width: '1000px',
            backgroundColor: 'blue'
        }} >
            hello
        </Box>
    );
};

export default FilterBar;